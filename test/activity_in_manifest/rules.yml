main:
	description: "Creates an activity in Android Manifest"
	version: "shandomain"

params:
	- id: name
	  desc: "O nome do servico"
	  regex: ""
	  type: select
	  required: false
	  options:
		- GET
		- POST
		- PUT
	
	- id: feature 
	  desc: "o nome da feature"
	  type: free
	  regex: ""

rules:
	- AndroidManifest.xml -> /app/AndroidManifest.xml
